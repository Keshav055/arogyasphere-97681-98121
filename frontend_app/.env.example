# Environment variable for API base URL (copy/rename to .env for use)
# For local dev, e.g.:
REACT_APP_API_BASE=http://localhost:3001
# For production, set to deployed backend API endpoint, e.g.:
# REACT_APP_API_BASE=https://your-backend-url.com
