import React from "react";

/**
 * Main landing dashboard for ArogyaMitr (goals, progress, widgets, insights).
 */
const DashboardPage = () => (
  <div className="container" style={{ marginTop: 30 }}>
    <h2>Welcome to ArogyaMitr</h2>
    <p>Your personalized health dashboard.</p>
    <div className="card" style={{ margin: "1em auto", maxWidth: 400 }}>
      <p>(Dashboard Widgets, Goals, Progress will appear here)</p>
    </div>
  </div>
);

export default DashboardPage;
